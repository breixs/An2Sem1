#include <stdio.h>

/**int main()
{
	//sume pare, produs impare
	int i, n, t[100], s = 0, p = 1;
	scanf("%d", &n);
	for (i = 0; i < n; i++)
	{
		scanf("%d", &t[i]);
	}
	for (i = 0; i < n; i++)
	{
		if (t[i] % 2 == 0)
			s = s + t[i];
		else
			p = p * t[i];
		
	}
	printf("%d", "%d\n", s, p);

	for (int i = 0; i < n; i++)
	{
		printf("%d", t[i]);
	}
	return 0;
 }*/

int main()
{
	int t[100], i, n, j, opt, p;
	scanf("%d", &n);
	scanf("%d", &t[i]);
	while (opt!=0)
		do 
		{
		switch(opt)
		case 1:
			for (i = 0; i < n; i++)
				for (int j = 0; j < n; j++)
					printf("%d", t[i][j]);
			printf("\t");

		case 2:
			for  ()



 }